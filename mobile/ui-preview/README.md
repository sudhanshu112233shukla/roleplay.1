# UI Preview

Open `index.html` to see a static mock of the app UI (dark mode, phone + tablet layout).

This is a visual preview only. The real UI is implemented in:

- Android Compose: `mobile/android/RoleplayAI/app/src/main/java/com/roleplayai/app/ui/`
- iOS SwiftUI: `mobile/ios/RoleplayAI/App/Views/`

